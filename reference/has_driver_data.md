# Check if mutation data has driver annotations

Check if mutation data has driver annotations

## Usage

``` r
has_driver_data(x)
```

## Arguments

- x:

  A CNAqc object containing mutation data.

## Value

Logical (`TRUE` if driver annotation columns exist, otherwise `FALSE`).
